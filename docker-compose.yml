services:
  devquote-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: npm run dev
    ports:
      - 3000:3000
    volumes:
      - ./:/usr/src/app
      - node_modules:/usr/src/app/node_modules
    container_name: devquote-dev

volumes:
  node_modules: